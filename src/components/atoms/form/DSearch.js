import React from 'react';

/**
 * 
 * @param props
 * @returns {JSX.Element}
 * @constructor
 */
function DSearch(props) {
  return (
    <input type={"search"} {...props}/>
  );
}

export default DSearch;